spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/cjhmdb
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  devtools: 
    livereload:
      enabled: true
logging:  level:    org.springframework.web: error
    com.cjhm.board: debug    